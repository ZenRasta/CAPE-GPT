# Example .env file for scripts
# Replace these with your actual credentials in .env (don't commit the actual .env file)

# Supabase Configuration (for data ingestion scripts)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-supabase-service-role-key

# OpenAI Configuration (for content analysis)
OPENAI_API_KEY=your-openai-api-key
OPENAI_LLM_MODEL=gpt-4o
OPENAI_EMBEDDING_MODEL=text-embedding-ada-002
